"use client";
import { useState } from "react";
import { Mail, Send } from "lucide-react";
import { site } from "@/lib/site";
export function ContactForm(){const [notice,setNotice]=useState(false);return <form className="surface p-6 sm:p-8" onSubmit={(event)=>{event.preventDefault();const data=new FormData(event.currentTarget);const subject=encodeURIComponent(String(data.get("subject")||"PixSqueeze question"));const body=encodeURIComponent(`Name: ${data.get("name")}\n\n${data.get("message")}`);setNotice(true);window.location.href=`mailto:${site.email}?subject=${subject}&body=${body}`;}}><div className="grid gap-5 sm:grid-cols-2"><label className="text-sm font-bold">Your name<input required name="name" autoComplete="name" className="mt-2 w-full rounded-xl border border-line bg-slate-50 px-4 py-3 font-normal"/></label><label className="text-sm font-bold">Email address<input required type="email" name="email" autoComplete="email" className="mt-2 w-full rounded-xl border border-line bg-slate-50 px-4 py-3 font-normal"/></label></div><label className="mt-5 block text-sm font-bold">Subject<input required name="subject" className="mt-2 w-full rounded-xl border border-line bg-slate-50 px-4 py-3 font-normal"/></label><label className="mt-5 block text-sm font-bold">Message<textarea required name="message" rows={6} className="mt-2 w-full resize-y rounded-xl border border-line bg-slate-50 px-4 py-3 font-normal"/></label><button className="mt-5 inline-flex items-center gap-2 rounded-xl bg-ink px-6 py-3 font-bold text-white"><Send size={17}/> Open email draft</button>{notice&&<p className="mt-4 flex items-center gap-2 text-sm font-semibold text-moss"><Mail size={16}/> Your mail app should open with this message. You can review it before sending.</p>}</form>}
