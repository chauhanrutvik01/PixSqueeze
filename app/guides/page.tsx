import type { Metadata } from "next";
import Link from "next/link";
import { ArrowRight, BookOpen } from "lucide-react";
import { guides } from "@/lib/guides";
import { openGraphImage } from "@/lib/seo";
export const metadata: Metadata = {title:"Image Compression & Resizing Guides",description:"Clear, practical guides to image formats, file-size limits, compression quality and preparing photos for online forms.",alternates:{canonical:"/guides"},openGraph:{title:"PixSqueeze Image Guides",description:"Practical help for smaller, better-prepared images.",url:"/guides",images:[openGraphImage]}};
export default function Page(){return <><section className="container-shell pb-12 pt-16 sm:pt-24"><span className="inline-grid h-12 w-12 place-items-center rounded-2xl bg-mint text-moss"><BookOpen size={22}/></span><p className="eyebrow mt-6">The PixSqueeze library</p><h1 className="display mt-3 max-w-3xl text-5xl sm:text-6xl">Understand your images, not just their file size.</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-slate-600">Plain-language notes on formats, dimensions, quality and the upload rules that bring you here.</p></section><section className="container-shell grid gap-5 md:grid-cols-2">{guides.map((guide,index)=><Link href={`/guides/${guide.slug}`} key={guide.slug} className={`group rounded-[1.75rem] border border-line bg-white p-7 transition hover:-translate-y-1 hover:border-moss hover:shadow-soft ${index===0?"md:col-span-2 md:grid md:grid-cols-[1fr_.45fr] md:gap-12 md:p-10":""}`}><div><span className="text-xs font-extrabold uppercase tracking-widest text-coral">{guide.category}</span><h2 className={`mt-4 font-display font-semibold leading-tight tracking-tight group-hover:text-moss ${index===0?"text-4xl":"text-3xl"}`}>{guide.title}</h2><p className="mt-4 leading-7 text-slate-600">{guide.description}</p></div><div className="mt-7 flex items-end justify-between md:mt-0"><span className="text-xs font-bold text-slate-400">{guide.readTime}</span><span className="grid h-10 w-10 place-items-center rounded-full bg-mint text-moss"><ArrowRight size={18}/></span></div></Link>)}</section></>}
