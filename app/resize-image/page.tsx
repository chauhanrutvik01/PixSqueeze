import type { Metadata } from "next";
import Link from "next/link";
import { ToolPage } from "@/components/ToolPage";
export const metadata: Metadata = { title:"Resize Image Online by Pixels — Free & Private",description:"Resize JPG, PNG, WebP or HEIC images to exact pixel dimensions in your browser. Free, private and watermark-free.",alternates:{canonical:"/resize-image"},openGraph:{title:"Resize Image Online by Pixels",description:"Change image dimensions privately in your browser.",url:"/resize-image"} };
export default function Page(){return <ToolPage mode="resize" eyebrow="Pixel-perfect resizing" title="Resize an image to" accent="exact dimensions" description="Set the width and height you need. Your browser creates a fresh copy while the original stays safely on your device." benefits={["Reads the source dimensions automatically","Optional aspect-ratio lock prevents stretching","No upload, account or watermark"]}><h2>Resize without the guesswork</h2><p>Pixel dimensions control how much visual information an image contains. A 4,000-pixel phone photo is useful for editing and print, but it is often excessive for a profile box, product grid or article thumbnail. Resizing it to the dimensions where it will actually appear reduces transfer time and memory use.</p><h2>How to resize an image online</h2><ol><li>Choose a JPG, PNG, WebP or HEIC image.</li><li>Enter a width or height. Keep the ratio lock on to preserve the original proportions.</li><li>Select Resize image, review the preview and download the new copy.</li></ol><h2>Dimensions and file size are different</h2><p>Resizing changes the pixel grid; compression changes how efficiently those pixels are stored. If a form specifies both 600 × 800 pixels and 100KB, resize first, then use the <Link href="/compress-image-to-100kb">100KB compressor</Link> for the file-weight limit.</p></ToolPage>}
